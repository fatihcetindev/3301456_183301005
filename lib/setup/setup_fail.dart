import 'package:flutter/material.dart';

class SetupFail extends StatefulWidget {
  const SetupFail({Key? key}) : super(key: key);

  @override
  _SetupFailState createState() => _SetupFailState();
}

class _SetupFailState extends State<SetupFail> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("setupfail"));
  }
}
